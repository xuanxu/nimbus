#Input files
input:
  training: training.data
  testing:  testing.data
  forest:   random_forest.yml

#Forest parameters
forest:
  forest_size: 3 #how many trees
  SNP_sample_size_mtry: 60 #mtry
  SNP_total_count: 200
  node_min_size: 5